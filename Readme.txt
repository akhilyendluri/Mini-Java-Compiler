Steps to run the program:

1. Run the following command 'make'.
2. Use the following command to run the driver 'cat <filename>.mjava | ./driver'
3. The output will be displayed on the terminal.
4. The output contains the tokens in the given file followed by the StringTable.
